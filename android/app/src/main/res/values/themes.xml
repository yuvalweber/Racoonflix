<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Theme -->
    <style name="Base.Theme.Netflix" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Common attributes for both light and dark themes -->
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

    <!-- Light Theme -->
    <style name="Theme.Netflix.Light" parent="Base.Theme.Netflix">
        <!-- Light mode customizations -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:statusBarColor">@color/gray</item>
        <item name="android:colorPrimary">@color/colorPrimary</item>
    </style>

    <!-- Dark Theme -->
    <style name="Theme.Netflix.Dark" parent="Base.Theme.Netflix">
        <!-- Dark mode customizations -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:statusBarColor">@color/black_opacity</item>
        <item name="android:colorPrimary">@color/colorPrimary</item>
    </style>
</resources>
